{
  "name": "oneepis",
  "version": "0.1.0",
  "private": true,
  "description": "Ficha clinica inteligente, modular y auditable.",
  "packageManager": "npm@11.13.0",
  "workspaces": [
    "apps/web",
    "packages/*"
  ],
  "scripts": {
    "bootstrap:ubuntu": "bash scripts/bootstrap-ubuntu.sh",
    "bootstrap:windows": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/bootstrap-windows.ps1",
    "dev:web": "npm --workspace apps/web run dev",
    "dev:api": "node scripts/python.js -m uvicorn oneepis_api.main:app --reload --app-dir apps/api/src",
    "check:api": "node scripts/python.js -m ruff check apps/api && node scripts/python.js -m pytest apps/api/tests",
    "check:web": "npm --workspace apps/web run typecheck && npm --workspace apps/web run lint && npm --workspace apps/web run build",
    "check:screens": "node scripts/check-screens.js",
    "check:contract": "node scripts/python.js apps/api/scripts/export_openapi.py && git diff --exit-code packages/contracts/openapi.json",
    "check:e2e": "npm --workspace apps/web run test:e2e",
    "check": "npm run check:api && npm run check:web && npm run check:contract && npm run check:e2e"
  },
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=11.13.0"
  }
}
