{
  "name": "oneepis",
  "version": "0.1.0",
  "private": true,
  "description": "Ficha clinica inteligente, modular y auditable.",
  "packageManager": "npm@11.13.0",
  "workspaces": [
    "apps/web",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm --workspace apps/web run dev",
    "dev:api": ".venv/bin/python -m uvicorn oneepis_api.main:app --reload --app-dir apps/api/src",
    "check:api": "python -m ruff check apps/api && python -m pytest apps/api/tests",
    "check:size": "node scripts/check-file-size.mjs",
    "check:web": "npm run check:size && npm --workspace apps/web run typecheck && npm --workspace apps/web run lint && npm --workspace apps/web run build",
    "check:contract": "python apps/api/scripts/export_openapi.py && node scripts/check-assistant-read-contract.mjs && git diff --exit-code packages/contracts/openapi.json",
    "check:e2e": "npm --workspace apps/web run test:e2e && npm --workspace apps/web run test:e2e:assistant-read",
    "check": "npm run check:api && npm run check:web && npm run check:contract && npm run check:e2e"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
