# Contracts

OpenAPI contracts exported from the FastAPI app.

Refresh with:

```bash
python apps/api/scripts/export_openapi.py
```

`openapi.json` is committed so agents and the frontend can inspect API shape without running the server.
